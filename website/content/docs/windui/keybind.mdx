---
title: Keybind
description: Create keyboard shortcut bindings in WindUI Modded
---

import { Tabs, TabsContent, TabsList, TabsTrigger } from "@/components/ui/tabs"
import { DynamicCodeBlock } from 'fumadocs-ui/components/dynamic-codeblock'
import TypeTable from '@/components/TypeTable';


## Keybind Element

Keybinds allow users to bind keyboard shortcuts to actions. They support all keyboard keys and modifier combinations.

<Tabs defaultValue="small">
    <TabsList>
        <TabsTrigger value="small">Basic</TabsTrigger>
        <TabsTrigger value="full">Full</TabsTrigger>
    </TabsList>
    <TabsContent value="small">
```luau
local Keybind = Tab:Keybind({
    Title = "Toggle UI",
    Value = "V", -- default key
    Callback = function(key)
        print("Keybind pressed:", key)
    end
})
```
    </TabsContent>
    <TabsContent value="full">
```luau
local Keybind = Tab:Keybind({
    Title = "Toggle UI",
    Desc = "Press this key to toggle the UI", -- optional
    Value = "V", -- default key. optional
    Locked = false, -- disable keybind. optional
    Flag = "toggle_ui_key", -- for config saving. optional
    Callback = function(key)
        print("Keybind activated, key:", key)
    end
})
```
    </TabsContent>
</Tabs>

## Keybind Parameters

<TypeTable
    data={[
        { name: "Title", type: "string", description: "Keybind label text", default: "Keybind" },
        { name: "Desc", type: "string?", description: "Keybind description", default: "nil" },
        { name: "Value", type: "string?", description: "Default key binding", default: "nil" },
        { name: "Locked", type: "boolean?", description: "Disable keybind interaction", default: "false" },
        { name: "Flag", type: "string?", description: "Configuration flag for saving", default: "nil" },
        { name: "Callback", type: "function?", description: "Function called when key is pressed", default: "nil" },
    ]}
/>

## Keybind Methods
<TypeTable 
    type={{
        ':SetTitle': {
            type: 'function',
            description: 'Change keybind label dynamically.',
            args: {
                Title: {
                    type: 'string',
                    description: 'New keybind label text.',
                }
            }
        },
        ':Set': {
            type: 'function',
            description: 'Programmatically set the keybind value.',
            args: {
                Value: {
                    type: 'Enum.KeyCode',
                    description: 'New keybind input.',
                }
            }
        },
        ':Lock': {
            type: 'function',
            description: 'Disable keybind interaction.',
            args: {}
        },
        ':Unlock': {
            type: 'function',
            description: 'Re-enable keybind interaction.',
            args: {}
        },
    }}
/>
## Related Documentation

- [Tab](./tab) - Tab container
- [Key System](./keysystem) - Advanced key system
